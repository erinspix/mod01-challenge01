# mod01-challenge01
Mod 01, challenge 01



README

## Project Overview
This project was assigned as a learning tool within a programming course. The focus was on solving a "help ticket" with an emphasis on accessibility.

## Assignment Scope
The assignment involved creating an accessible HTML page,  using the code provided and implementing semantic HTML

## Enhancements
I enhanced the code to improve browser accessibility by:

- Using semantic HTML elements for better content structure.

## Learning Outcomes
I learned the importance of:

- **Semantic HTML:** Using elements like `<header>`, `<nav>`, `<main>`, `<section>`, and `<footer>` for meaningful content structure.

## Conclusion
I need to consolidate the css. That will be in my second attempt.

